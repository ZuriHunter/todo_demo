==To Do Application

To start the Application
1. bundle install
2. rake db:migrate
3. rails server

The interface for that app hasn't been drawn out too much but it is meant to understand the core functionality.
